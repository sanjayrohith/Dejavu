# dejavu

**Memory that lets coding agents continue instead of start over.**

Dejavu gives a coding agent a fast, repository-scoped memory between sessions,
stored in one inspectable SQLite file. Recall is local, bounded, and cited.

Status: early work in progress. Full documentation to follow.
